#include <stdio.h>
#include <locale.h>

main()
{
	int x=0;
	if(x=0)printf("%d",x);//diferente de algumas linguagens ela funciona somente com um sinal de = para verificar a iguadade.
	if(x=1);//verifica se x =1
	if(x=-10);//verifica se x = -10
		
}
