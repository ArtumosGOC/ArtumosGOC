#include<stdio.h>//libs
void main()//metodo vazio main()
{
	int valor;//var int
	valor = 1;//valor recebe 1
	if (valor < 0)//se valor menor que 0
	{
		printf("Primeira comparação\n");//imprime mensagem
	}
	if (valor == 0)//se valor igual que 0
	{
		printf("Segunda comparação\n");//imprime mensagem
	}
	if (valor > 0)//se valor maior que 0
	{
		printf("Terceira comparação\n");//imprime mensagem
	}
}
